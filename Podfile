source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'

target 'iPokedex' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iPokedex
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON'

  target 'iPokedexTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iPokedexUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
